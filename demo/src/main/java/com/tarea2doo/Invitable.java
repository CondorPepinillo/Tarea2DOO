package com.tarea2doo;
/**
 * Interfaz Invitable
 * Esta interfaz define el método invitar, que permite invitar a un empleado a una reunión.
 */
public interface Invitable {
    public void invitar(Reunion reunion);
}